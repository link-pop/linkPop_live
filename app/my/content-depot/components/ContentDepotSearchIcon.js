"use client";

import { Search } from "lucide-react";
import { ICONBUTTON_CLASS } from "@/lib/utils/constants";

const ContentDepotSearchIcon = () => {
  return <Search size={24} className={ICONBUTTON_CLASS} />;
};

export default ContentDepotSearchIcon;
